'use strict';

const fs = require('fs');
const sysPath = require('path');
const { promisify } = require('util');
const isBinaryPath = require('is-binary-path');
const {
  isWindows,
  isLinux,
  EMPTY_FN,
  EMPTY_STR,
  KEY_LISTENERS,
  KEY_ERR,
  KEY_RAW,
  HANDLER_KEYS,
  EV_CHANGE,
  EV_ADD,
  EV_ADD_DIR,
  EV_ERROR,
  STR_DATA,
  STR_END,
  BRACE_START,
  STAR
} = require('./constants');

const THROTTLE_MODE_WATCH = 'watch';

const open = promisify(fs.open);
const stat = promisify(fs.stat);
const lstat = promisify(fs.lstat);
const close = promisify(fs.close);
const fsrealpath = promisify(fs.realpath);

const statMethods = { lstat, stat };

// TODO: emit errors properly. Example: EMFILE on Macos.
const foreach = (val, fn) => {
  if (val instanceof Set) {
    val.forEach(fn);
  } else {
    fn(val);
  }
};

const addAndConvert = (main, prop, item) => {
  let container = main[prop];
  if (!(container instanceof Set)) {
    main[prop] = container = new Set([container]);
  }
  container.add(item);
};

const clearItem = cont => key => {
  const set = cont[key];
  if (set instanceof Set) {
    set.clear();
  } else {
    delete cont[key];
  }
};

const delFromSet = (main, prop, item) => {
  const container = main[prop];
  if (container instanceof Set) {
    container.delete(item);
  } else if (container === item) {
    delete main[prop];
  }
};

const isEmptySet = (val) => val instanceof Set ? val.size === 0 : !val;

/**
 * @typedef {String} Path
 */

// fs_watch helpers

// object to hold per-process fs_watch instances
// (may be shared across chokidar FSWatcher instances)

/**
 * @typedef {Object} FsWatchContainer
 * @property {Set} listeners
 * @property {Set} errHandlers
 * @property {Set} rawEmitters
 * @property {fs.FSWatcher=} watcher
 * @property {Boolean=} watcherUnusable
 */

/**
 * @type {Map<String,FsWatchContainer>}
 */
const FsWatchInstances = new Map();

/**
 * Instantiates the fs_watch interface
 * @param {String} path to be watched
 * @param {Object} options to be passed to fs_watch
 * @param {Function} listener main event handler
 * @param {Function} errHandler emits info about errors
 * @param {Function} emitRaw emits raw event data
 * @returns {fs.FSWatcher} new fsevents instance
 */
function createFsWatchInstance(path, options, listener, errHandler, emitRaw) {
  const handleEvent = (rawEvent, evPath) => {
    listener(path);
    emitRaw(rawEvent, evPath, {watchedPath: path});

    // emit based on events occurring for files from a directory's watcher in
    // case the file's watcher misses it (and rely on throttling to de-dupe)
    if (evPath && path !== evPath) {
      fsWatchBroadcast(
        sysPath.resolve(path, evPath), KEY_LISTENERS, sysPath.join(path, evPath)
      );
    }
  };
  try {
    return fs.watch(path, options, handleEvent);
  } catch (error) {
    errHandler(error);
  }
}

/**
 * Helper for passing fs_watch event data to a collection of listeners
 * @param {Path} fullPath absolute path bound to fs_watch instance
 * @param {String} type listener type
 * @param {*=} val1 arguments to be passed to listeners
 * @param {*=} val2
 * @param {*=} val3
 */
const fsWatchBroadcast = (fullPath, type, val1, val2, val3) => {
  const cont = FsWatchInstances.get(fullPath);
  if (!cont) return;
  foreach(cont[type], (listener) => {
    listener(val1, val2, val3);
  });
};

/**
 * Instantiates the fs_watch interface or binds listeners
 * to an existing one covering the same file system entry
 * @param {String} path
 * @param {String} fullPath absolute path
 * @param {Object} options to be passed to fs_watch
 * @param {Object} handlers container for event listener functions
 */
const setFsWatchListener = (path, fullPath, options, handlers) => {
  const {listener, errHandler, rawEmitter} = handlers;
  let cont = FsWatchInstances.get(fullPath);

  /** @type {fs.FSWatcher=} */
  let watcher;
  if (!options.persistent) {
    watcher = createFsWatchInstance(
      path, options, listener, errHandler, rawEmitter
    );
    return watcher.close.bind(watcher);
  }
  if (cont) {
    addAndConvert(cont, KEY_LIST�ка листе „$1“: унос је занемарен јер је уједно укључен у смерницу SpellcheckLanguage.Повезан је:Смернице за одсуство:Није активнопрофил екрана за пријављивањеКопирај вредност смерница „$1“Копирај као JSONЕвиденције смерницаИнформације о верзијиИзвези евиденције у JSONОсвежи евиденцијеПримењене су смернице за тестирање. Прегледајте их и успоставите почетне вредности на edge://policy/test. Администратор смернице неће бити примењене.Није могуће делити поверљиви садржајОва смерница аутоматски онемогућава синхронизацију на свим уређајима за: $1.Мора бити наведен један подразумевани добављач претраге.Може се навести само један подразумевани добављач претраге.Може да се наведе највише 100 добављача претраге.Ова вредност смерница се занемарује јер се за укључивање функције надгледања лозинки захтева пристанак појединачног корисника. Можете тражити од корисника у вашој организацији да оду у Поставке > Профил > Лозинка и да укључе ову функцију.Ова смерница је застарела и више не функционише.Смернице су поново учитанеУправљање уређајимаActive Directory домен је придружен: $1Azure AD домен је придружен: $1MDM уписан: $1Администратор је блокирао ову страницу$1 на серверу $2Услуга композитора штампањаПобољшана приватност за огласе у прегледачу EdgeПокренули смо нове функције приватности које вам омогућавају више избора у вези са огласима који вам се приказују.Теме огласа омогућавају сајтовима да вам приказују релевантне огласе, истовремено штитећи вашу историју прегледања и идентитет. Edge може да бележи теме које вас занимају на основу ваше недавне историје прегледања. Касније, неки од сајтова које посећујете може од прегледача Edge затражити релевантне теме како би персонализовао огласе који вам се приказују.У поставкама можете видети теме огласа и блокирати оне које не желите да делите са сајтовима. Edge такође аутоматски брише теме огласа старије од 4 седмице.Више о темама огласаУ поставкама прегледача Edge у сваком тренутку можете да се предомислите<b>Који подаци се користе:</b> Теме огласа засноване су на вашој недавној историји прегледања, на листи сајтова које сте посетили користећи Edge на овом уређају.<b>Како користимо ове податке:</b> Edge бележи теме које вас заинтересују током прегледања. Ознаке тема унапред су дефинисане и обухватају ствари као што су Уметност и забава, Куповина и Спорт и рекреација. Касније, неки од сајтова које посећујете може од прегледача Edge затражити неколико ваших тема (али не и историју прегледања) како би персонализовао огласе који вам се приказују.<b>Како можете управљати подацима:</b> Edge аутоматски брише теме старије од 4 седмице. Тема се током даљег прегледања може поново појавити на листи. Можете и блокирати теме које не желите да Edge дели са другим сајтовима и у сваком тренутку можете искључити теме у поставкама прегледача Edge.Сазнајте више о томе како Microsoft штити ваше податке у нашим Смерницама за приватност.Сада су доступне друге функције приватности за рекламеПокренули смо нове начине да ограничимо шта сајтови могу да сазнају о вама када вам приказују персонализоване огласе, на пример:Огласи које предлаже сајт помажу у заштити историје прегледања и идентитета и у исто време омогућавају сајтовима да вам прикажу релевантне огласе. На основу ваше активности, сајт који сте посетили може да предложи повезане огласе док прегледате. У поставкама можете да прикажете листу ових сајтова и блокирате оне које не желите.Уз мерење огласа, ограничени типови података се деле између сајтова како би се измериле перформансе њихових огласа, као што је време приказивања огласа.Више о огласима које предлаже сајт и мерењу огласаМожете да извршите промене у поставкама прегледача Edge.Огласи које предлаже сајт<b>Који подаци се користе:</b> Ваша активност на сајту који управо прегледате користећи Edge на овом уређају.<b>Како сајтови користе ове податке:</b> Сајтови могу да складиште информације помоћу прегледача Edge о стварима које вам се свиђају. На пример, ако посетите сајт у вези са тренингом за маратон, сајт може закључити да вас занимају патике за трчање. Касније, ако посетите неки други сајт, тај сајт вам може приказати оглас за патике за трчање које је предложио први сајт.<b>Како можете да управљате својим подацима::</b> Edge аутоматски брише сајтове старије од 30 дана. Сајт који посетите поново може да се поново појави на листи.Такође, можете и да блокирате сајт из предложених огласа као и да у било ком тренутку искључите огласе које предлаже сајт у Edge поставкама.Мерење ефикасности огласаСајтови које посећујете могу затражити од прегледача Edgno more processing is needed for this entry.
 */
async _handleSymlink(entry, directory, path, item) {
  if (this.fsw.closed) {
    return;
  }
  const full = entry.fullPath;
  const dir = this.fsw._getWatchedDir(directory);

  if (!this.fsw.options.followSymlinks) {
    // watch symlink directly (don't follow) and detect changes
    this.fsw._incrReadyCount();

    let linkPath;
    try {
      linkPath = await fsrealpath(path);
    } catch (e) {
      this.fsw._emitReady();
      return true;
    }

    if (this.fsw.closed) return;
    if (dir.has(item)) {
      if (this.fsw._symlinkPaths.get(full) !== linkPath) {
        this.fsw._symlinkPaths.set(full, linkPath);
        this.fsw._emit(EV_CHANGE, path, entry.stats);
      }
    } else {
      dir.add(item);
      this.fsw._symlinkPaths.set(full, linkPath);
      this.fsw._emit(EV_ADD, path, entry.stats);
    }
    this.fsw._emitReady();
    return true;
  }

  // don't follow the same symlink more than once
  if (this.fsw._symlinkPaths.has(full)) {
    return true;
  }

  this.fsw._symlinkPaths.set(full, true);
}

_handleRead(directory, initialAdd, wh, target, dir, depth, throttler) {
  // Normalize the directory name on Windows
  directory = sysPath.join(directory, EMPTY_STR);

  if (!wh.hasGlob) {
    throttler = this.fsw._throttle('readdir', directory, 1000);
    if (!throttler) return;
  }

  const previous = this.fsw._getWatchedDir(wh.path);
  const current = new Set();

  let stream = this.fsw._readdirp(directory, {
    fileFilter: entry => wh.filterPath(entry),
    directoryFilter: entry => wh.filterDir(entry),
    depth: 0
  }).on(STR_DATA, async (entry) => {
    if (this.fsw.closed) {
      stream = undefined;
      return;
    }
    const item = entry.path;
    let path = sysPath.join(directory, item);
    current.add(item);

    if (entry.stats.isSymbolicLink() && await this._handleSymlink(entry, directory, path, item)) {
      return;
    }

    if (this.fsw.closed) {
      stream = undefined;
      return;
    }
    // Files that present in current directory snapshot
    // but absent in previous are added to watch list and
    // emit `add` event.
    if (item === target || !target && !previous.has(item)) {
      this.fsw._incrReadyCount();

      // ensure relativeness of path is preserved in case of watcher reuse
      path = sysPath.join(dir, sysPath.relative(dir, path));

      this._addToNodeFs(path, initialAdd, wh, depth + 1);
    }
  }).on(EV_ERROR, this._boundHandleError);

  return new Promise(resolve =>
    stream.once(STR_END, () => {
      if (this.fsw.closed) {
        stream = undefined;
        return;
      }
      const wasThrottled = throttler ? throttler.clear() : false;

      resolve();

      // Files that absent in current directory snapshot
      // but present in previous emit `remove` event
      // and are removed from @watched[directory].
      previous.getChildren().filter((item) => {
        return item !== directory &&
          !current.has(item) &&
          // in case of intersecting globs;
          // a path may have been filtered out of this readdir, but
          // shouldn't be removed because it matches a different glob
          (!wh.hasGlob || wh.filterPath({
            fullPath: sysPath.resolve(directory, item)
          }));
      }).forEach((item) => {
        this.fsw._remove(directory, item);
      });

      stream = undefined;

      // one more time for any missed in case changes came in extremely quickly
      if (wasThrottled) this._handleRead(directory, false, wh, target, dir, depth, throttler);
    })
  );
}

/**
 * Read directory to add / remove files from `@watched` list and re-read it on change.
 * @param {String} dir fs path
 * @param {fs.Stats} stats
 * @param {Boolean} initialAdd
 * @param {Number} depth relative to user-supplied path
 * @param {String} target child path targeted for watch
 * @param {Object} wh Common watch helpers for this path
 * @param {String} realpath
 * @returns {Promise<Function>} closer for the watcher instance.
 */
async _handleDir(dir, stats, initialAdd, depth, target верификациони кôд.Позовите банку на број $1 да бисте добили кôд за верификацију.Банка вам шаље кôд за верификацију на адресу $1.У наставку унесите шифру коју сте добили од банке.Банка ће позвати ваш број мобилног телефона $1 са кодом за верификацију.Банка вам шаље кôд за верификацију на број мобилног телефона $1.Проверите да ли испуњавате услове за плаћање током временаАутоматско попуњавање није функционисалоТрајало је превише временаКупите одмах. Платите на рате уз услугу Klarna. $1Комерцијалну картицу Klarna Visa® издаје банка Sutton Bank, чланица FDIC, у складу са лиценцом корпорације Visa U.S.A Inc. Картица Klarna користи технологију предузећа Marqeta. Корисничка служба $1Могућа је примена Klarna услуге и накнада за кашњењеKlarna услуга одложеног плаћања на $1Уштедите новац уз Klarna.Ако наставите, пристајете на $1 и прихватате да се укупан износ ваше набавке заједно са локацијом куповине дели са предузећем Klarna, у складу са $2. Ако сте недавно одобрени за трансакцију од стране компаније Klarna, могу се применити услови плаћања са тим одобрењем.Управљајте начинима плаћањаАко наставите, прихватате $1, $2 и $3 у вези са начином на који се поступа са вашим подацима.Иди на следећу страницуИдите на страницу $1Иди на претходну страницуУпишите сеПлаћајте безбедно виртуелном картицомПодесите виртуелну картицу и освојите 20 Microsoft Rewards поенаИздавалац картице може послати кôд за верификацију или затражити безбедносни кôд картице како би потврдио да сте то ви.Виртуелна картица скрива детаље картице од трговаца када купујете на мрежи. Ако код некога од њих дође до нарушавања безбедности података, ваши подаци остају заштићени. $1Ако наставите, прихватате $1.Апликација за мобилно банкарствоКорисничка услуга банкеРачун у банциТелефонски позивПотврдите безбедносни кôд и покушајте поново.Ваша банка захтева да верификујете CVV безбедносну шифру повезану са вашим $1 који се завршава на $2.Издавалац картице је наишао на проблем приликом слања кода. Захтевајте други кôд или користите други метод верификације.Издавалац картице је имао проблем пр�