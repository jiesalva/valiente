�е оның идентификаторын білгісі келеді. Сайт қандай қауіпсіздік кілтін қолданғаныңызды нақты білетін болады.$1 бағдарламасына кіру үшін құрылғыны пайдалану керек пе?InPrivate режимінен шыққаннан кейін кіру деректері осы құрылғыда сақталады. Кейінірек сіз бұл веб-сайтқа құрылғыңыз арқылы кіре аласыз.Құрылғыларды басқару$1 үшін кіру кілтін жасауБұл кіру кілті тек Windows Hello ішінде сақталадыБұл кіру кілті тек Windows Hello ішінде сақталады. Ол сіз барлық InPrivate терезелерін жапқаннан кейін осы құрылғыда қалады.Кіру кілтін жасау$1 үшін кіру кілтін жасау жолын таңдаңызКіру кілтін пайдалану$1 кіру кілті қай құрылғыда бар екенін таңдаңыз$1 кіру кілтін пайдалануКіру кілтін таңдаңыз$1 үшін қандай кіру кілтін пайдаланғыңыз келеді?Телефонда немесе планшетте кіру кілтін жасау$1 үшін кіру кілтін жасағыңыз келетін құрылғы камерасы арқылы осы QR кодты сканерлеңізБасқа құрылғыдан кіру кілтін пайдалану керек пе?$1 үшін пайдаланғыңыз келетін кіру кілті бар құрылғы арқылы осы QR кодты сканерлеңізБасқа құрылғыны пайдалануБасқа кіру кілтін пайдалануUSB қауіпсіздік кілтінде $1 үшін кіру кілтін жасағыңыз келсе, оны қазір қосыңыз және түртіңіз$1 кіру кілті USB қауіпсіздік кілтінде болса, оны қазір қосыңыз және түртіңіз$1 үшін кіру кілтін таңдауОсы құрылғыда"$1" құрылғысынанБасқа құрылғылардаChrome профиліңізденWindows Hello бағдарламасынанЖаңа бүйірлік тақтада іздеу нәтижелерін тезірек салыстыруҚосымша іздеу нәтижелерін көруБүйірлік тақтадағы іздеуді ашуДеректерді тазалау үшін барлық InPrivate терезелерін жабыңызInPrivate шолу сеансындағы деректер ғана тазартыладыТерезелерді жабуInPrivate режимінде шолу кезінде сіз кірген сайттар сақталмайдыInPrivate режимінде шолу тарихын құрылғыңыздан тазалау үшін барлық InPrivate терезелерін жабыңызInPrivate терезесін жабуПайдалыПайдалы емес{NUM_FILES, plural,
        =0 {Осы деректердің ұйымның қауіпсіздік саясаттарымен сәйкестігі тексерілуде...}
        =1 {Осы файлдың ұйымның қауіпсіздік саясаттарымен сәйкестігі тексерілуде...}
      one {Осы файлдардың ұйымның қауіпсіздік саясаттарымен сәйкестігі тексерілуде...}
      other {Осы файлдардың ұйымның қауіпсіздік саясаттарымен сәйкестігі тексерілуде...}}Бұл құжаттың ұйымыңыздың қауіпсіздік туралы саясаттарына сәйкестігі тексерілуде…{NUM_FILES, plural,
        =0 {Қауіпсіздік тексерістері орындалды. Деректеріңіз жүктеп салынатын болады.}
        =1 {Қауіпсіздік тексерістері орындалды. Файлыңыз жүктеп салынатын болады.}
      one {Қауіпсіздік тексерістері орындалды. Файлдарыңыз жүктеп салынатын болады.}
      other {Қауіпсіздік тексерістері орындалды. Файлдарыңыз жүктеп салынатын болады.}}Қауіпсіздікті тексерулер орындалды. Құжатыңыз басып шығарылады.{NUM_FILES, plural,
        =0 {Бұл деректер немесе құрылғыңыз ұйымыңыздың кейбір қауіпсіздік саясаттарына сай келмейді. Нені түзету керек екенін әкімшіден сұраңыз.}
        =1 {Бұл файл немесе құрылғыңыз ұйымыңыздың кейбір қауіпсіздік саясаттарына сай келмейді. Нені түзету керек екенін әкімшіден сұраңыз.}
      one {Бұл файлдар ұйымыңыздың кейбір қауіпсіздік саясаттарына сай келмейді. Нені түзету керек екенін әкімшіден сұраңыз.}
      other {Бұл файлдар ұйымыңыздың кейбір қауіпсіздік саясаттарына сай келмейді. Нені түзету керек екенін әкімшіден сұраңыз.}}Бұл құжат немесе құрылғыңыз ұйымыңыздың кейбір қауіпсіздік саясаттарына сай келмейді. Нені түзету керек екенін әкімшіден сұраңыз.Сонда да жалғастыру{NUM_FILES, plural,
        =1 {Қауіпсіздік тексерісін орындау үшін бұл файл көлемі тым үлкен. Көлемі 50 Мбайтқа дейінгі файлдарды жүктеп салуға болады.}
      one {Қауіпсіздік тексерісін орындау үшін бұл файлдардың кейбірінің көлемі тым үлкен. Көлемі 50 Мбайтқа дейінгі файлдарды жүктеп салуға болады.}
      other {Қауіпсіздік тексерісін орындау үшін бұл файлдардың кейбірінің көлемі тым үлкен. Көлемі 50 Мбайтқа дейінгі файлдарды жүктеп салуға болады.}}Бұл құжат қауіпсіздікті тексеру үшін тым үлкен. 50 МБ-қа дейінгі құжаттарды басып шығаруға болады.{NUM_FILES, plural,
        =1 {Бұл файл шифрланған. Иесінен файл шифрын ашуды сұраңыз.}
      one {Осы файлдардың кейбірі шифрланған. Иесінен олардың шифрын ашуды сұраңыз.}
      other {Осы файлдардың кейбірі шифрланған. Иесінен олардың шифрын ашуды сұраңыз.}}Әкімшіңіз былай дейді: "$1".$1 файлы құпиялылық немесе қауіпті деректерді қамтиды. Әкімшіңіздің айтуы: "$2".Осы деректерді кері жүктеу себебін енгізіңіз:Осы деректерді жүктеп алу себебін енгізіңіз:Сканерлеу орындалуда, қазір ашу керек пе?Белгісіз файл қауіпті мазмұн бойынша сканерленуде.Файлды ашпас бұрын сканерлеу қажет пе?$1 қауіпті болуы мүмкін. Кеңейтілген қорғау мүмкіндігі зиянды бағдарламаның бар-жоқтығын тексеру үшін бұл файлды Microsoft h
     * the cwd, then this ends up being equivalent to the fullpathPosix()
     * On posix systems, this is identical to relative().
     */
    relativePosix(): string;
    /**
     * The fully resolved path string for this Path entry
     */
    fullpath(): string;
    /**
     * On platforms other than windows, this is identical to fullpath.
     *
     * On windows, this is overridden to return the forward-slash form of the
     * full UNC path.
     */
    fullpathPosix(): string;
    /**
     * Is the Path of an unknown type?
     *
     * Note that we might know *something* about it if there has been a previous
     * filesystem operation, for example that it does not exist, or is not a
     * link, or whether it has child entries.
     */
    isUnknown(): boolean;
    isType(type: Type): boolean;
    getType(): Type;
    /**
     * Is the Path a regular file?
     */
    isFile(): boolean;
    /**
     * Is the Path a directory?
     */
    isDirectory(): boolean;
    /**
     * Is the path a character device?
     */
    isCharacterDevice(): boolean;
    /**
     * Is the path a block device?
     */
    isBlockDevice(): boolean;
    /**
     * Is the path a FIFO pipe?
     */
    isFIFO(): boolean;
    /**
     * Is the path a socket?
     */
    isSocket(): boolean;
    /**
     * Is the path a symbolic link?
     */
    isSymbolicLink(): boolean;
    /**
     * Return the entry if it has been subject of a successful lstat, or
     * undefined otherwise.
     *
     * Does not read the filesystem, so an undefined result *could* simply
     * mean that we haven't called lstat on it.
     */
    lstatCached(): PathBase | undefined;
    /**
     * Return the cached link target if the entry has been the subject of a
     * successful readlink, or undefined otherwise.
     *
     * Does not read the filesystem, so an undefined result *could* just mean we
     * don't have any cached data. Only use it if you are very sure that a
     * readlink() has been called at some point.
     */
    readlinkCached(): PathBase | undefined;
    /**
     * Returns the cached realpath target if the entry has been the subject
     * of a successful realpath, or undefined otherwise.
     *
     * Does not read the filesystem, so an undefined result *could* just mean we
     * don't have any cached data. Only use it if you are very sure that a
     * realpath() has been called at some point.
     */
    realpathCached(): PathBase | undefined;
    /**
     * Returns the cached child Path entries array if the entry has been the
     * subject of a successful readdir(), or [] otherwise.
     *
     * Does not read the filesystem, so an empty array *could* just mean we
     * don't have any cached data. Only use it if you are very sure that a
     * readdir() has been called recently enough to still be valid.
     */
    readdirCached(): PathBase[];
    /**
     * Return true if it's worth trying to readlink.  Ie, we don't (yet) have
     * any indication that readlink will definitely fail.
     *
     * Returns false if the path is known to not be a symlink, if a previous
     * readlink failed, or if the entry does not exist.
     */
    canReadlink(): boolean;
    /**
     * Return true if readdir has previously been successfully called on this
     * path, indicating that cachedReaddir() is likely valid.
     */
    calledReaddir(): boolean;
    /**
     * Returns true if the path is known to not exist. That is, a previous lstat
     * or readdir failed to verify its existence when that would have been
     * expected, or a parent entry was marked either enoent or enotdir.
     */
    isENOENT(): boolean;
    /**
     * Return true if the path is a match for the given path name.  This handles
     * case sensitivity and unicode normalization.
     *
     * Note: even on case-sensitive systems, it is **not** safe to test the
     * equality of the `.name` property to determine whether a given pathname
     * matches, due to unicode normalization mismatches.
     *
     * Always use this method instead of testing the `path.name` property
     * directly.
     */
    isNamed(n: string): boolean;
    /**
     * Return the Path object corresponding to the target of a symbolic link.
     *
     * If the Path is not a symbolic link, or if the readlink call fails for any
     * reason, `undefined` is returned.
     *
     * Result is cached, and thus may be outdated if the filesystem is mutated.
     */
    readlink(): Promise<PathBase | undefined>;
    /**
     * Synchronous {@link PathBase.readlink}
     */
    readlinkSync(): PathBase | undefined;
    /**
     * Call lstat() on this Path, and update all known information that can be
     * determined.
     *
     * Note that unlike `fs.lstat()`, the returned value does not contain some
     * information, such as `mode`, `dev`, `nlink`, and `ino`.  If that
     * information is required, you will need to call `fs.lstat` yourself.
     *
     * If the Path refers to a nonexistent file, or if the lstat call fails for
     * any reason, `undefined` is returned.  Otherwise the updated Path object is
     * returned.
     *
     * Results are cached, and thus may be out of date if the filesystem is
     * mutated.
     */
    lstat(): Promise<PathBase | undefined>;
    /**
     * synchronous {@link PathBase.lstat}
     */
    lstatSync(): PathBase | undefined;
    /**
     * Standard node-style callback interface to get list of directory entries.
     *
     * If the Path cannot or does not contain any children, then an empty array
     * is returned.
     *
     * Results are cached, and thus may be out of date if the filesystem is
     * mutated.
     *
     * @param cb The callback called with (er, entries).  Note that the `er`
     * param is somewhat extraneous, as all readdir() errors are handled and
     * simply result in an empty set of entries being returned.
     * @param allowZalgo Boolean indicating that immediately known results should
     * *not* be deferred with `queueMicrotask`. Defaults to `false`. Release
     * zalgo at your peril, the dark pony lord is devious and unforgiving.
     */
    readdirCB(cb: (er: NodeJS.ErrnoException | null, entries: PathBase[]) => any, allowZalgo?: boolean): void;
    /**
     * Return an array of known child entries.
     *
     * If the Path cannot or does not contain any children, then an empty array
     * is returned.
     *
     * Results are cached, and thus may be out of date if the filesystem is
     * mutated.
     */
    readdir(): Promise<PathBase[]>;
    /**
     * synchronous {@link PathBase.readdir}
     */
    readdirSync(): PathBase[];
    canReaddir(): boolean;
    shouldWalk(dirs: Set<PathBase | undefined>, walkFilter?: (e: PathBase) => boolean): boolean;
    /**
     * Return the Path object corresponding to path as resolved
     * by realpath(3).
     *
     * If the realpath call fails for any reason, `undefined` is returned.
     *
     * Result is cached, and thus may be outdated if the filesystem is mutated.
     * On success, returns a Path object.
     */
    realpath(): Promise<PathBase | undefined>;
    /**
     * Synchronous {@link realpath}
     */
    realpathSync(): PathBase | undefined;
    /**
     * Internal method to mark this Path object as the scurry cwd,
     * called by {@link PathScurry#chdir}
     *
     * @internal
     */
    [setAsCwd](oldCwd: PathBase): void;
}
/**
 * Path class used on win32 systems
 *
 * Uses `'\\'` as the path separator for returned paths, either `'\\'` or `'/'`
 * as the path separator for parsing paths.
 */
export declare class PathWin32 extends PathBase {
    /**
     * Separator for generating path strings.
     */
    sep: '\\';
    /**
     * Separator for parsing path strings.
     */
    splitSep: RegExp;
    /**
     * Do not create new Path objects directly.  They should always be accessed
     * via the PathScurry class or other methods on the Path class.
     *
     * @internal
     */
    constructor(name: string, type: number | undefined, root: PathBase | undefined, roots: {
        [k: string]: PathBase;
    }, nocase: boolean, children: ChildrenCache, opts: PathOpts);
    /**
     * @internal
     */
    newChild(name: string, type?: number, opts?: PathOpts): PathWin32;
    /**
     * @internal
     */
    getRootString(path: string): string;
    /**
     * @internal
     */
    getRoot(rootPath: string): PathBase;
    /**
     * @internal
     */
    sameRoot(rootPath: string, compare?: string): boolean;
}
/**
 * Path class used on all posix systems.
 *
 * Uses `'/'` as the path separator.
 */
export declare class PathPosix extends PathBase {
    /**
     * separator for parsing path strings
     */
    splitSep: '/';
    /**
     * separator for generating path strings
     */
    sep: '/';
    /**
     * Do not create new Path objects directly.  They should always be accessed
     * via the PathScurry class or other methods on the Path class.
     *
     * @internal
     */
    constructor(name: string, type: number | undefined, root: PathBase | undefined, roots: {
        [k: string]: PathBase;
    }, nocase: boolean, children: ChildrenCache, opts: PathOpts);
    /**
     * @internal
     */
    getRootString(path: string): string;
    /**
     * @internal
     */
    getRoot(_rootPath: string): PathBase;
    /**
     * @internal
     */
    newChild(name: string, type?: number, opts?: PathOpts): PathPosix;
}
/**
 * Options that may be provided to the PathScurry constructor
 */
export interface PathScurryOpts {
    /**
     * perform case-insensitive path matching. Default based on platform
     * subclass.
     */
    nocase?: boolean;
    /**
     * Number of Path entries to keep in the cache of Path child references.
     *
     * Setting this higher than 65536 will dramatically increase the data
     * consumption and construction time overhead of each PathScurry.
     *
     * Setting this value to 256 or lower will significantly reduce the data
     * consumption and construction time overhead, but may also reduce resolve()
     * and readdir() performance on large filesystems.
     *
     * Default `16384`.
     */
    childrenCacheSize?: number;
    /**
     * An object that overrides the built-in functions from the fs and
     * fs/promises modules.
     *
     * See {@link FSOption}
     */
    fs?: FSOption;
}
/**
 * The base class for all PathScurry classes, providing the interface for path
 * resolution and filesystem operations.
 *
 * Typically, you should *not* instantiate this class directly, but rather one
 * of the platform-specific classes, or the exported {@link PathScurry} which
 * defaults to the current platform.
 */
export declare abstract class PathScurryBase {
    #private;
    /**
     * The root Path entry for the current working directory of this Scurry
     */
    root: PathBase;
    /**
     * The string path for the root of this Scurry's current working directory
     */
    rootPath: string;
    /**
     * A collection of all roots encountered, referenced by rootPath
     */
    roots: {
        [k: string]: PathBase;
    };
    /**
     * The Path entry corresponding to this PathScurry's current working directory.
     */
    cwd: PathBase;
    /**
     * Perform path comparisons case-insensitively.
     *
     * Defaults true on Darwin and Windows systems, false elsewhere.
     */
    nocase: boolean;
    /**
     * The path separator used for parsing paths
     *
     * `'/'` on Posix systems, either `'/'` or `'\\'` on Windows
     */
    abstract sep: string | RegExp;
    /**
     * This class should not be instantiated directly.
     *
     * Use PathScurryWin32, PathScurryDarwin, PathScurryPosix, or PathScurry
     *
     * @internal
     */
    constructor(cwd: string | URL | undefined, pathImpl: typeof win32 | typeof posix, sep: string | RegExp, { nocase, childrenCacheSize, fs, }?: PathScurryOpts);
    /**
     * Get the depth of a provided path, string, or the cwd
     */
    depth(path?: Path | string): number;
    /**
     * Parse the root portion of a path string
     *
     * @internal
     */
    abstract parseRootPath(dir: string): string;
    /**
     * create a new Path to use as root during construction.
     *
     * @internal
     */
    abstract newRoot(fs: FSValue): PathBase;
    /**
     * Determine whether a given path string is absolute
     */
    abstract isAbsolute(p: string): boolean;
    /**
     * Return the cache of child entries.  Exposed so subclasses can create
     * child Path objects in a platform-specific way.
     *
     * @internal
     */
    childrenCache(): ChildrenCache;
    /**
     * Resolve one or more path strings to a resolved string
     *
     * Same interface as require('path').resolve.
     *
     * Much faster than path.resolve() when called multiple times for the same
     * path, because the resolved Path objects are cached.  Much slower
     * otherwise.
     */
    resolve(...paths: string[]): string;
    /**
     * Resolve one or more path strings to a resolved string, returning
     * the posix path.  Identical to .resolve() on posix systems, but on
     * windows will return a forward-slash separated UNC path.
     *
     * Same interface as require('path').resolve.
     *
     * Much faster than path.resolve() when called multiple times for the same
     * path, because the resolved Path objects are cached.  Much slower
     * otherwise.
     */
    resolvePosix(...paths: string[]): string;
    /**
     * find the relative path from the cwd to the supplied path string or entry
     */
    relative(entry?: PathBase | string): string;
    /**
     * find the relative path from the cwd to the supplied path string or
     * entry, using / as the path delimiter, even on Windows.
     */
    relativePosix(entry?: PathBase | string): string;
    /**
     * Return the basename for the provided string or Path object
     */
    basename(entry?: PathBase | string): string;
    /**
     * Return the dirname for the provided string or Path object
     */
    dirname(entry?: PathBase | string): string;
    /**
     * Return an array of known child entries.
     *
     * First argument may be either a string, or a Path object.
     *
     * If the Path cannot or does not contain any children, then an empty array
     * is returned.
     *
     * Results are cached, and thus may be out of date if the filesystem is
     * mutated.
     *
     * Unlike `fs.readdir()`, the `withFileTypes` option defaults to `true`. Set
     * `{ withFileTypes: false }` to return strings.
     */
    readdir(): Promise<PathBase[]>;
    readdir(opts: {
        withFileTypes: true;
    }): Promise<PathBase[]>;
    readdir(opts: {
        withFileTypes: false;
    }): Promise<string[]>;
    readdir(opts: {
        withFileTypes: boolean;
    }): Promise<PathBase[] | string[]>;
    readdir(entry: PathBase | string): Promise<PathBase[]>;
    readdir(entry: PathBase | string, opts: {
        withFileTypes: true;
    }): Promise<PathBase[]>;
    readdir(entry: PathBase | string, opts: {
        withFileTypes: false;
    }): Promise<string[]>;
    readdir(entry: PathBase | string, opts: {
        withFileTypes: boolean;
    }): Promise<PathBase[] | string[]>;
    /**
     * synchronous {@link PathScurryBase.readdir}
     */
    readdirSync(): PathBase[];
    readdirSync(opts: {
        withFileTypes: true;
    }): PathBase[];
    readdirSync(opts: {
        withFileTypes: false;
    }): string[];
    readdirSync(opts: {
        withFileTypes: boolean;
    }): PathBase[] | string[];
    readdirSync(entry: PathBase | string): PathBase[];
    readdirSync(entry: PathBase | string, opts: {
        withFileTypes: true;
    }): PathBase[];
    readdirSync(entry: PathBase | string, opts: {
        withFileTypes: false;
    }): string[];
    readdirSync(entry: PathBase | string, opts: {
        withFileTypes: boolean;
    }): PathBase[] | string[];
    /**
     * Call lstat() on the string or Path object, and update all known
     * information that can be determined.
     *
     * Note that unlike `fs.lstat()`, the returned value does not contain some
     * information, such as `mode`, `dev`, `nlink`, and `ino`.  If that
     * information is required, you will need to call `fs.lstat` yourself.
     *
     * If the Path refers to a nonexistent file, or if the lstat call fails for
     * any reason, `undefined` is returned.  Otherwise the updated Path object is
     * returned.
     *
     * Results are cached, and thus may be out of date if the filesystem is
     * mutated.
     */
    lstat(entry?: string | PathBase): Promise<PathBase | undefined>;
    /**
     * synchronous {@link PathScurryBase.lstat}
     */
    lstatSync(entry?: string | PathBase): PathBase | undefined;
    /**
     * Return the Path object or string path corresponding to the target of a
     * symbolic link.
     *
     * If the path is not a symbolic link, or if the readlink call fails for any
     * reason, `undefined` is returned.
     *
     * Result is cached, and thus may be outdated if the filesystem is mutated.
     *
     * `{withFileTypes}` option defaults to `false`.
     *
     * On success, returns a Path object if `withFileTypes` option is true,
     * otherwise a string.
     */
    readlink(): Promise<string | undefined>;
    readlink(opt: {
        withFileTypes: false;
    }): Promise<string | undefined>;
    readlink(opt: {
        withFileTypes: true;
    }): Promise<PathBase | undefined>;
    readlink(opt: {
        withFileTypes: boolean;
    }): Promise<PathBase | string | undefined>;
    readlink(entry: string | PathBase, opt?: {
        withFileTypes: false;
    }): Promise<string | undefined>;
    readlink(entry: string | PathBase, opt: {
        withFileTypes: true;
    }): Promise<PathBase | undefined>;
    readlink(entry: string | PathBase, opt: {
        withFileTypes: boolean;
    }): Promise<string | PathBase | undefined>;
    /**
     * synchronous {@link PathScurryBase.readlink}
     */
    readlinkSync(): string | undefined;
    readlinkSync(opt: {
        withFileTypes: false;
    }): string | undefined;
    readlinkSync(opt: {
        withFileTypes: true;
    }): PathBase | undefined;
    readlinkSync(opt: {
        withFileTypes: boolean;
    }): PathBase | string | undefined;
    readlinkSync(entry: string | PathBase, opt?: {
        withFileTypes: false;
    }): string | undefined;
    readlinkSync(entry: string | PathBase, opt: {
        withFileTypes: true;
    }): PathBase | undefined;
    readlinkSync(entry: string | PathBase, opt: {
        withFileTypes: boolean;
    }): string | PathBase | undefined;
    /**
     * Return the Path object or string path corresponding to path as resolved
     * by realpath(3).
     *
     * If the realpath call fails for any reason, `undefined` is returned.
     *
     * Result is cached, and thus may be outdated if the filesystem is mutated.
     *
     * `{withFileTypes}` option defaults to `false`.
     *
     * On success, returns a Path object if `withFileTypes` option is true,
     * otherwise a string.
     */
    realpath(): Promise<string | undefined>;
    realpath(opt: {
        withFileTypes: false;
    }): Promise<string | undefined>;
    realpath(opt: {
        withFileTypes: true;
    }): Promise<PathBase | undefined>;
    realpath(opt: {
        withFileTypes: boolean;
    }): Promise<PathBase | string | undefined>;
    realpath(entry: string | PathBase, opt?: {
        withFileTypes: false;
    }): Promise<string | undefined>;
    realpath(entry: string | PathBase, opt: {
        withFileTypes: true;
    }): Promise<PathBase | undefined>;
    realpath(entry: string | PathBase, opt: {
        withFileTypes: boolean;
    }): Promise<string | PathBase | undefined>;
    realpathSync(): string | undefined;
    realpathSync(opt: {
        withFileTypes: false;
    }): string | undefined;
    realpathSync(opt: {
        withFileTypes: true;
    }): PathBase | undefined;
    realpathSync(opt: {
        withFileTypes: boolean;
    }): PathBase | string | undefined;
    realpathSync(entry: string | PathBase, opt?: {
        withFileTypes: false;
    }): string | undefined;
    realpathSync(entry: string | PathBase, opt: {
        withFileTypes: true;
    }): PathBase | undefined;
    realpathSync(entry: string | PathBase, opt: {
        withFileTypes: boolean;
    }): string | PathBase | undefined;
    /**
     * Asyncтын модульдерЖаңа чаттың қосылатын модульдерін осында қосылғандай табу үшін қайта тексеріңіз.Қосылатын модульдермен Bing чатын жақсартуКейбір қосылатын модульдер алдын ала орнатылған және жүйеге кірген құрылғыларда қолжетімді болады.оқытушылық кеңесті жабуҚазір байқап көруБұл қосылатын модуль қауіпсіз әрекетке байланысты ажыратылды.$1 қосымша мәзір түймешігі$1 модулінің өшірулі қосымша мәзір түймешігіЧаттың қосылатын модулін жоюMicrosoft тіркелгісінен "$1" жою қажет пе?АлуБірыңғай сақтау орныБүйірлік тақтаны босатуБүйірлік тақтаны бекітуБүйірлік жолаққа бетті қосуСайттарды іздеуБүйірлік жолаққа кез келген сайтты немесе веб-бетті қосуға боладыБүйірлік жолаққа кез келген сайтты немесе веб-бетті қосуға болады. Ағымдағы бетті қосыңыз, танымал сайтты іздеңіз немесе сайтты бүйірлік жолаққа қосу үшін төменде үздік сайтты таңдаңыз.Бүйірлік жолаққа кез келген сайтты немесе веб-бетті қосуға болады. Ағымдағы бетті қосыңыз немесе төменде танымал сайтты іздеңіз.Бүйірлік жолаққа кез келген сайтты немесе веб-бетті қосуға болады. Ағымдағы бетті қосыңыз немесе төменде үздік сайтты таңдаңыз.Хабарландыруларды кейінге қалдыру$1 — $2Қайта іске қосуға дейін көрсетпеуОсы сайт үшін көрсетпеуБарлығын кейінге қалдыруБарлығын кейінге қалдырудан бас тартуБасқарылатын браузерлер үшін өшірілген.Сіздің ұйымыңыз тарапынан басқарылады.Бүйірлік тақта енді көрініп тұрБүйірлік тақта енді жасырылдыАвтономды бүйірлік жолақШығуБүйірлік жолақты автоматты түрде іске қосуАлға іздеуЖұмыс кеңістігін өңдеуЖұмыс кеңістігін жою«$1» жұмыс кеңістігін жойғыңыз келетініне сенімдісіз бе? Бұл жұмыс кеңістігі сіз және оның барлық мүшелері үшін жойылады. Осы жұмыс кеңістігімен байланысты журналыңыз бен таңдаулыларыңыз да жойылады.«$1» жұмыс кеңістігін жойғыңыз келетініне сенімдісіз бе? Осы жұмыс кеңістігімен байланысты журналыңыз бен таңдаулыларыңыз да жойылады.$1 жұмыс кеңістігінен шығу қажет екеніне сенімдісіз бе? Бұл жұмыс кеңістігі құрылғыңыздан жойылады. Бұл жұмыс кеңістігімен байланысты журнал мен таңдаулылар осы жұмыс кеңістігінің басқа мүшелеріне әлі де қолжетімді болады.«$1» жұмыс кеңістігін жою керек пе?«$1» жұмыс кеңістігінен шығу керек пе?Жұмыс кеңістіктері арқылы шолу әрекеттеріңізді ұйымдастырыңыз және оңай бірлесіп шолу үшін басқалармен бөлісіңіз.{NUM_TABS, plural,
    =1 {{NUM_TABS} қойынша}
    one {{NUM_TABS} қойынша}
    other {{NUM_TABS} қойынша}}{NUM_TABS, plural,
      =1 {{NUM_TABS} ортақ қойынша}
      one {{NUM_TABS} ортақ қойынша}
      other {{NUM_TABS} ортақ қойынша}}Жұмыс кеңістігі деректерді басқа құрылғылардан қол жеткізу және бірлескен жұмыс қатысушыларымен бөлісу үшін онлайн режимінде сақтайды.Жаңасын жасауЖаңа жұмыс кеңістігін жасау$1 жұмыс кеңістігіЖаңа жұмыс кеңістігіАтауы жоқ жұмыс кеңістігіЖаңа жұмыс кеңістігіне жылжытқыңыз келетін қойыншаларды таңдаңыз:Жаңа бос жұмыс кеңістігін жасауНемесеЖұмыс кеңістіктерінің алдын ала қарау нұсқасыОртақ пайдалануға арналған сілтеме жасалуда…Қате: ортақ пайдалануға арналған сілтемені жасау мүмкін болмадыСілтемені көшіру сәтсіз болдыБөлісу сілтемесі алынудаБөлісу сілтемесін жасауБарлық жұмыс кеңістігі үлгілеріЖұмыс кеңістігіңізді осы іріктелген үлгілердің бірімен бастаңызКүте тұрыңыз. Жұмыс кеңістіктеріңіз жүктелуде…$1 ашылуда…Жаңа жұмыс кеңістігіңіз жасалуда…«Жұмыс кеңістіктерін жасыру» түймешігі«Жұмыс кеңістіктерін көрсету» түймешігіЖұмыс кеңістігіне шақыруЖұмыс кеңістігі чатын ашуЖұмыс кеңістігінен шығуҚате: жұмыс кеңістіктерін жүктеу мүмкін емесҚате: $1 жұмыс кеңістігін жүктеу мүмкін болмадыҚате: $1 жұмыс кеңістігін жүктеу мүмкін болмады. Осы жұмыс кеңістігін ашу үшін, Microsoft Edge браузерінің соңғы нұсқасына жаңартыңыз.Жұмыс кеңістігіне қосылуда, күте тұрыңызҚате: қосылу сәтсіз аяқталды. Дұрыс және толық URL мекенжайын пайдаланып жатқаныңызға көз жеткізіңіз.Қате: жұмыс кеңістігіне қосылу мүмкін болмады. Осы жұмыс кеңістігін ашу үшін, Microsoft Edge браузерінің соңғы нұсқасына жаңартыңыз.Қате: күтпеген қате орын алды. Қате коды: $1Қате: URL сұрауын өңдеу кезіндегі қатеге байланысты қосылу мүмкін емес. Әрекетті қайталаңыз. Қате коды: $1Қате: жұмыс кеңістігіне қосылу мүмкін болмады. $1 тіркелгісімен кіріп, қайталап көріңіз.Қате: жұмыс кеңістігіне қосылу мүмкін болмады. Ағымдағы профиль үшін Edge жұмыс кеңістіктері қосылғанына көз жеткізіңіз.Қате: жаңа жұмыс кеңістігін жасау мүмкін емесҚате: енді жұмыс кеңістігіне қатынасыңыз жоқ. Ол енгізулер тізімінен жойылады.Жұмыс кеңістіктеріне қанағаттандыңыз ба?Жұмыс кеңістіктеріне қош келдіңіз$1 сәтті жаңартылды.$1 сәтті жойылды.Жұмыс кеңістіктерінің тізімі жаңартылудаЖұмыс кеңістіктері туралы қосымша ақпаратЖұмыс кеңістіктері құпиялылығы туралы қосымша ақпаратMicrosoft Edge жұмыс кеңістіктерінің алдын ала қарау нұсқасы енді қолжетімді!Edge Workspace арқылы бірге шолыңыз.Келесі сапарыңызды және т.б. жоспарлау үшін Edge Workspace арқылы бірге шолыңыздар.Алдын ала қарау нұсқасына қосылуЖұмыс кеңістіктері. Хабарландыруды тексеріңіз.$1 жұмыс кеңістігі терезесі ашық.Барлық ашық жұмыс кеңістігі терезелері жабылуда.$1 жұмыс кеңістігі терезесі жабылуда.Бұл жұмыс кеңістігі Microsoft Edge шолғышының соңғы нұсқасына жаңартылған. Оны пайдалануды жалғастыру және барлық өзгерістерді сақтау үшін қазір жаңартыңыз. Шолғыш жаңарту кезінде қайта іске қосылады.Жұмыс кеңістігін қайта іске қосуЖұмыс кеңістігінің қатесіБұл жұмыс кеңістігі ажыратылған және болашақтағы барлық өзгерістер сақталмайды. Оның байланысын қалпына келтіру үшін жұмыс кеңістігін қайта іске қосыңыз немесе жұмыс кеңістігін жабыңыз.Бұл жұмыс кеңістігі ажыратылған және барлық болашақтағы өзгерістер сақталмайды.Жұмыс кеңістігін жабуБраузерді жаңартуЖурналдарды тазалауJSON файлына экспорттауЭкспортталған JSON файлы жұмыс кеңістігінің навигация журналын қамтиды. Қатысушының электрондық пошта мекенжайлары сияқты пайдаланушыны анықтауға болатын ақпарат қосылмайды.Журналдарды анонимді ету және таңдалған файлға жазу. Оны аяқтау үшін біраз уақыт қажет болуы мүмкін.JSON файлына экспорттау сәтті аяқталды.JSON файлына экспорттау сәтсіз аяқталды.Жұмыс кең�